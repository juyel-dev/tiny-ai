experiment: E003
base_checkpoint: checkpoints/e002.pt
dataset:
  source: local E002 TinyStories train split + authored chat examples
  format: JSONL
  purpose: instruction/chat fine-tuning
model:
  inherited_from_checkpoint: true
training:
  batch_size: 8
  learning_rate: 0.0001
  steps: 1000
  eval_every: 100
  seed: 1337
  response_only_loss: true
